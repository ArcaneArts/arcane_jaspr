# Arcane Jaspr KB

Knowledge-base chrome renderers for Arcane Jaspr themes. This package keeps the
server-side `arcane_lexicon` dependency out of the web-safe theme packages.

Import `package:arcane_jaspr_kb/arcane_jaspr_kb.dart` and pass the renderer for
your selected theme to `arcane_lexicon`.
